import 'package:flutter/material.dart';

class PostSucceeded extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('success')));
  }
}

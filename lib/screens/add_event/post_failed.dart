import 'package:flutter/material.dart';

class PostFailed extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('failed')));
  }
}

mutation createChat($chatInput: ChatInput!) {
  createChat(options: $chatInput) {
    ok
    nodes {
      id
      text
      createdAt
      author {
      ...EventUser
      }
    }
    errors {
      field
      message
    }
  }
}

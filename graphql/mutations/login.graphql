
mutation login($email: String!, $password: String!) {
    login {
        nodes {
            ...User
        }
        errors {
            field
            message
        }
        jwt {
            accessToken
            refreshToken
        }
        ok
    }
}
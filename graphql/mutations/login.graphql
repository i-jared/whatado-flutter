mutation login($userInput: UserInput!) {
  login(options: $userInput) {
    ok
    nodes {
      id
    }
    errors {
      field
      message
    }
    jwt {
      accessToken
      refreshToken
    }
  }
}

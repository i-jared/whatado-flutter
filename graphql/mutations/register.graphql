mutation register($email: String!, $password: String!) {
  register {
    nodes {
      id
    }
    errors {
      field
      message
    }
    jwt {
      accessToken
      refreshToken
    }
  }
}

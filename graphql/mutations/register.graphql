mutation register($email: String!, $password: String!) {
    register {
        nodes {
            ...User
        }
        errors {
            field
            message
        }
        jwt {
            accessToken
            refreshToken
        }
        ok
    }
}
mutation createReferral($phone: String!, $eventId: Int, $groupId: Int) {
  createReferral(phone: $phone, eventId: $eventId, groupId: $groupId) {
    ok
    nodes 
    errors {
      field
      message
    }
  }
}

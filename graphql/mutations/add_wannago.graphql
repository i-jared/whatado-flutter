mutation addWannago($eventId: Int!, $userId: Int!) {
  addWannago(eventId: $eventId, userId: $userId) {
    ok
    nodes {
      ...PublicEventFields 
    }
    errors {
      field
      message
    }
  }
}

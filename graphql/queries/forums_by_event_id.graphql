query forumsByEventId($eventIds: [Int!]!) {
  forumsByEventId(ids: $eventIds) {
    ok
    errors {
      field
      message
    }
    nodes {
      id
      userNotifications {
        id
        notifications
        muted
      }
      event {
        id
      }
    }
  }
}

query events($dateRange: DateRangeInput!, $take: Int!, $skip: Int!) {
  events(dateRange: $dateRange, take: $take, skip: $skip) {
    ok
    nodes {
      ...EventFields
    }
    errors {
      field
      message
    }
  }
}
